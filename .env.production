# Supabase public client config — baked into the Vite build.
# The publishable/anon key is RLS-protected and ships in the browser bundle by
# design, so it is safe to commit (it is NOT a secret).
VITE_SUPABASE_URL=https://bfxhcevcqkoxlsqlbcny.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_AwuQZIUeZIVShs50HIFVpw_6fPK0FUb
